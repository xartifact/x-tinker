{
  "name": "x-tinker",
  "version": "0.1.0",
  "private": false,
  "description": "Self-healing pipeline: runtime error → auto fix → deploy",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "cd apps/server && bun run dev",
    "dev:ui": "cd apps/ui && bun run dev",
    "dev": "bun run dev:server & bun run dev:ui",
    "build:ui": "cd apps/ui && bun run build",
    "typecheck": "bun run --filter './packages/*' typecheck && bun run --filter './apps/*' typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.14",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "packageManager": "bun@1.0.0"
}
